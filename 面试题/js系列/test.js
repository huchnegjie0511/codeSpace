// 中阔号中的值会被认为是变量加入
// 例如：test.js?name=123&age=456
// 会被认为是一个对象，对象的属性名是name和age，属性值分别是123和456
// 所以我们可以通过这种方式来传递参数
// 例如：test.js?fn=fn
// 这样我们就可以通过window.fn来调用fn函数
// 但是我们需要注意的是，这种方式传递的参数是字符串，所以我们需要通过eval()函数来转化为函数
// 例如：eval(fn)()
// 这样就可以调用fn函数了
