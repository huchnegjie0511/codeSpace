let str ='abcdefg'

console.log(str[2])
console.log(str.at(2))//字符串特定位置字符输出

console.log(str.startsWith('ab'))//j进行判定字符串开头是否有括号内指定字符

console.log(str.endsWith('aa'))//进行判定字符串末尾是否有括号内指定字符

console.log(str.padStart(20,'0'))//在未达到某些数量大小的字符串前面进行补充后面的字符
console.log(str.padEnd(20,'0'))//在未达到某些数量大小的字符串后面进行补充后面的字符

